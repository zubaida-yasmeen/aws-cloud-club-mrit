
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- Helper Functions ---
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }

    // --- Collection Rules ---

    match /events/{eventId} {
      allow get, list: if true;
      allow write: if isAdmin();
    }

    match /team/{teamMemberId} {
      allow get, list: if true;
      allow write: if isAdmin();
    }

    match /projects/{projectId} {
      allow get, list: if true;
      allow write: if isAdmin();
    }

    match /registrations/{registrationId} {
      // Allow anyone to create a registration
      allow create: if true;
      // Only admins can view or manage registrations
      allow get, list, update, delete: if isAdmin();
    }
    
    // Legacy support for members collection if still used
    match /members/{memberId} {
      allow create: if request.resource.data.id == memberId;
      allow get, list, update, delete: if isAdmin();
    }
  }
}
